<%= form_for(@holidaytype) do |f| %>
  <% if @holidaytype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holidaytype.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% @holidaytype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
